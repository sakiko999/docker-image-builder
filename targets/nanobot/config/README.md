# Configuration templates

Only non-secret templates belong in this directory. A later build step copies them to `/opt/nanobot-overlay/config`; it never overwrites a mounted `~/.nanobot` runtime directory.
