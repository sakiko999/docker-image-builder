# Patches

Place source changes in `.patch` files in this directory. The build applies them in lexicographic order. A conflict stops the build before publication. Keep this directory empty when no source changes are needed.
